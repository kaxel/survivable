<%= form_with(model: possession) do |form| %>
  <% if possession.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(possession.errors.count, "error") %> prohibited this possession from being saved:</h2>

      <ul>
        <% possession.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :bonus %>
    <%= form.text_field :bonus %>
  </div>

  <div class="field">
    <%= form.label :current_game_id %>
    <%= form.text_field :current_game_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
